{
  "name": "frp-pacman",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "server": "parcel src/index.html --no-cache",
    "build": "rm -rf dist/* && parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.2.0",
    "eslint": "^9.2.0",
    "gh-pages": "^6.1.1",
    "globals": "^15.2.0",
    "parcel": "^2.12.0"
  }
}
